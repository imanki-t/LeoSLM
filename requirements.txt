# LeoSLM "Aether" — requirements.txt
# Built by Unmuted
# Install: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────────────────────
torch>=2.1.0
numpy>=1.24.0
tqdm>=4.66.0

# ── HuggingFace ────────────────────────────────────────────────────────────────
transformers>=4.40.0          # Adafactor, PreTrainedTokenizerFast, AutoTokenizer
datasets>=2.18.0              # data streaming from HuggingFace Hub
tokenizers>=0.19.0            # BPE tokenizer training in prep_data.py

# ── Evaluation ────────────────────────────────────────────────────────────────
scikit-learn>=1.3.0           # AUROC in eval/evaluate.py

# ── TPU (Kaggle TPU v5e-8) ────────────────────────────────────────────────────
# torch_xla is pre-installed on Kaggle TPU instances.
# Uncomment to install manually on a Cloud TPU VM:
# torch_xla>=2.1.0

# ── RAG / Knowledge layer (leo_rag.py) — all optional ────────────────────────
# Pick one vector store backend:
# chromadb>=0.4.0             # pip install chromadb
# faiss-cpu>=1.7.4            # pip install faiss-cpu  (faster for large indexes)

# Semantic embeddings:
# sentence-transformers>=2.7.0  # pip install sentence-transformers

# Web search:
# duckduckgo-search>=5.0.0      # pip install duckduckgo-search

# Document parsing (DocumentIndexer):
# pypdf>=4.0.0                  # PDFs
# python-docx>=1.1.0            # .docx files
# beautifulsoup4>=4.12.0        # HTML files
