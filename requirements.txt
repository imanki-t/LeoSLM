# LeoSLM Aether — Python dependencies
# Install: pip install -r requirements.txt --break-system-packages
#
# NOTE: torch_xla is PRE-INSTALLED on Kaggle TPU v5e-8 notebooks.
#       DO NOT pip install torch_xla — it will conflict with the Kaggle build.
#       DO NOT pip install tensorflow — it conflicts with torch_xla on TPU.

# ── Core ─────────────────────────────────────────────────────────────────────
torch>=2.1.0
numpy>=1.24.0
tqdm>=4.66.0

# ── Tokeniser & datasets ──────────────────────────────────────────────────────
transformers>=4.40.0
datasets>=2.18.0
tokenizers>=0.19.0

# ── Evaluation ────────────────────────────────────────────────────────────────
scikit-learn>=1.3.0

# ── API server ────────────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0

# ── Web search (for ToolRegistry built-in tools) ──────────────────────────────
duckduckgo-search>=6.0.0

# ── RAG / vector store ────────────────────────────────────────────────────────
sentence-transformers>=2.7.0
faiss-cpu>=1.7.4
chromadb>=0.4.0

# ── Document parsing (for DocumentIndexer) ───────────────────────────────────
pypdf>=4.0.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0

# ── Config parsing — BUG FIX: was missing, used in leo_rag.py ────────────────
pyyaml>=6.0.0

# ── Safety filter extras ──────────────────────────────────────────────────────
# (unicodedata, re, hashlib, collections are stdlib — no install needed)
